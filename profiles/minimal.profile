# minimal -- the smallest useful footprint.
#
# Shell integration and git config, nothing else. This is what you want on a
# box you are only passing through, or one with no package manager access.
shell
git
